# Core
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
pyyaml>=6.0
pydantic>=2.0
tqdm>=4.66
rich>=13.0
pyarrow>=14.0

# Split
iterative-stratification>=0.1.7

# Vector search: faiss-gpu는 CUDA 환경에 따라 설치가 까다로워서 기본은 cpu fallback으로 둔다.
faiss-cpu>=1.7.4

# Vision/Text models
torch>=2.1.0
torchvision>=0.16.0
# transformers 5.x는 open_clip(BiomedCLIP BertModel)/Qwen 로딩과 호환되지 않는다. 4.x로 고정.
transformers>=4.45.0,<5
accelerate>=0.30.0
pillow>=10.0.0
sentence-transformers>=3.0.0
open-clip-torch>=3.0.0
# HF 데이터셋 스트리밍 (vrag build --dataset indiana_hf 등)
datasets>=2.14.0
# bitsandbytes(4bit)는 CUDA 전용 -> macOS에서는 설치/사용 안 함
bitsandbytes>=0.43.0; platform_system != "Darwin"

# Optional report metrics
bert-score>=0.3.13
rouge-score>=0.1.2

# Optional prompt optimization
textgrad>=0.1.5

# Dev
pytest>=8.0.0
